import { test } from '../../src/fixtures/pages.fixture';

// See docs/test-cases/storefront/TC-MISC.md. POM support: SpecialsPage,
// ManufacturerPage, VoucherPage (src/pages/storefront/) are scaffolded with
// basic navigation but not yet exercised against the live app.

test.fixme('TC-SPECIAL-001 specials page lists only products with an active discount', async () => {});
test.fixme('TC-MANUFACTURER-002 brand page filters products to the selected manufacturer', async () => {});
test.fixme('TC-VOUCHER-003 gift voucher purchase flow completes successfully', async () => {});
