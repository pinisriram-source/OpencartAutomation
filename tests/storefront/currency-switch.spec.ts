import { test } from '../../src/fixtures/pages.fixture';

// See docs/test-cases/storefront/TC-MISC.md. HeaderNav.switchCurrency()
// already exists (src/pages/storefront/HeaderNav.ts) and is usable as-is.

test.fixme('TC-CURRENCY-004 currency selection persists across navigation and affects checkout total', async () => {});
