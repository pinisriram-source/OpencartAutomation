import { test } from '../../src/fixtures/pages.fixture';

// See docs/test-cases/admin/TC-ADMIN-ORDERS.md (TC-ADMIN-RET-*) and
// docs/test-cases/storefront/TC-ACCOUNT.md (TC-ACCOUNT-012). Requires a
// storefront "submit a return" flow (not yet built) feeding into
// ReturnListPage / ReturnDetailPage (src/pages/admin/), which are
// scaffolded but unverified against the live app.

test.fixme('TC-ACCOUNT-012 return request submission is accepted with required fields', async () => {});
test.fixme('TC-ADMIN-RET-007 return request submitted by a customer appears in Admin Returns', async () => {});
test.fixme('TC-ADMIN-RET-008 admin can update a return\'s status', async () => {});
